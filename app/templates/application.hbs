{{#paper-toolbar}}
    {{#paper-toolbar-tools}}
      {{#paper-button iconButton=true}}
        {{paper-icon "menu"}}
      {{/paper-button}}
      <h2>
        Braintrust BetTracker 2.0
      </h2>

      <span class="flex"></span>
      {{#link-to 'bets' class="button"}}
        Bets
      {{/link-to}}
      <span class="flex"></span>
      {{#link-to 'submitbet' class="button"}}
        Submit Bet
      {{/link-to}}
      <span class="flex"></span>
      {{#paper-button iconButton=true}}
        {{paper-icon "favorite"}}
      {{/paper-button}}
      {{#paper-button iconButton=true}}
        {{paper-icon "more_vert"}}
      {{/paper-button}}
    {{/paper-toolbar-tools}}
  {{/paper-toolbar}}

    <div class="layout-row mainContent">
    <div class="flex-15">
    </div>
    <div class="flex-70">

    {{outlet}}

    </div>
    <div class="flex"></div>
    </div>
